interface:
  display_name: "Librechat.ai"
  short_description: "Repo-specific patterns and workflows for librechat.ai"
  default_prompt: "Use the librechat.ai repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true